{
  "name": "rimac-appointment",
  "version": "1.0.0",
  "description": "Backend Serverless para reto Rimac",
  "scripts": {
    "start": "npm run build && serverless offline start --stage DESA --region us-east-1",
    "build": "tsc",
    "deploy": "npm run build && serverless deploy --stage DESA --region us-east-1",
    "test": "jest --detectOpenHandles --forceExit"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.136",
    "@types/jest": "^29.5.3",
    "@types/uuid": "^10.0.0",
    "jest": "^29.7.0",
    "serverless": "^3.40.0",
    "serverless-offline": "^13.4.0",
    "serverless-plugin-typescript": "^2.1.5",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.598.0",
    "@aws-sdk/client-sns": "^3.913.0",
    "@aws-sdk/client-sqs": "^3.913.0",
    "@aws-sdk/lib-dynamodb": "^3.913.0",
    "uuid": "^9.0.1"
  }
}
